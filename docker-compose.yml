services:
  watchparty-v8:
    image: muaeabudhabi/watchparty-v8:latest
    restart: unless-stopped
    labels:
      com.centurylinklabs.watchtower.enable: 'true'
      org.titan.app: watchparty-v8
    environment:
    - NODE_ENV=production
    - LOG_LEVEL=info
    - PUID=99
    - PGID=100
    - UMASK=022
    - DATABASE_URL=postgresql://watchparty-v8:changeme@watchparty-v8_postgres:5432/watchparty-v8
    - REDIS_URL=redis://watchparty-v8_redis:6379
    volumes:
    - /mnt/user/appdata/watchparty-v8/data:/data
    - /mnt/user/appdata/watchparty-v8/config:/config
    logging:
      driver: json-file
      options: &id001
        max-size: 10m
        max-file: '3'
    networks:
    - watchparty-v8_net
    depends_on:
      watchparty-v8_postgres:
        condition: service_healthy
      watchparty-v8_redis:
        condition: service_healthy
  watchparty-v8_postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
    - POSTGRES_DB=watchparty-v8
    - POSTGRES_USER=watchparty-v8
    - POSTGRES_PASSWORD=changeme
    volumes:
    - watchparty-v8_postgres_data:/var/lib/postgresql/data
    networks:
    - watchparty-v8_net
    logging:
      driver: json-file
      options: *id001
    healthcheck:
      test:
      - CMD-SHELL
      - pg_isready -U ${POSTGRES_USER:-app}
      interval: 30s
      timeout: 5s
      retries: 5
  watchparty-v8_redis:
    image: redis:7-alpine
    restart: unless-stopped
    environment: []
    volumes:
    - watchparty-v8_redis_data:/data
    networks:
    - watchparty-v8_net
    logging:
      driver: json-file
      options: *id001
    healthcheck:
      test:
      - CMD
      - redis-cli
      - ping
      interval: 30s
      timeout: 5s
      retries: 5
networks:
  watchparty-v8_net:
    driver: bridge
volumes:
  watchparty-v8_data:
    driver: local
  watchparty-v8_postgres_data:
    driver: local
  watchparty-v8_redis_data:
    driver: local
