# Unraid Templates for watchparty-v8

XML templates for Unraid Community Applications.

## Install
Copy `my-watchparty-v8.xml` to `/boot/config/plugins/dockerMan/templates-user/` on Unraid,
then: Docker tab → Add Container → select from template list.
